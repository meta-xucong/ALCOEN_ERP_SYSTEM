# 轻量级ERP系统技术方案文档

## 1. 项目概述

### 1.1 项目背景
建设一套基于Web的简易ERP订单管理系统，部署于公司VPS，供内部员工使用。

### 1.2 用户需求
- 通过IP:端口访问Web界面
- 支持订单录入（产品名称、公司名称、产品号、发货量等）
- 数据持久化存储
- 支持Excel/CSV导出（预设格式模板）
- 支持多条件检索查询
- 查看产品信息及历史订单

### 1.3 使用场景
- 用户规模：≤10人
- 并发量：低
- 部署环境：Ubuntu VPS（专用）

---

## 2. 技术选型

### 2.1 核心技术栈

| 层级 | 技术 | 版本 | 选型理由 |
|-----|------|------|---------|
| **后端语言** | Python | 3.10+ | 开发效率高，数据处理能力强，Excel生态丰富 |
| **Web框架** | Flask | 2.3+ | 轻量级，学习成本低，插件丰富，适合中小型项目 |
| **数据库** | SQLite | 3.x | 零配置、单文件存储、备份简单、足够支撑10人使用 |
| **ORM** | SQLAlchemy | 2.0+ | 数据库操作简洁，支持迁移 |
| **Excel生成** | pandas + openpyxl | 最新版 | Python最强数据处理组合，支持复杂格式预设 |
| **前端** | HTML5 + Bootstrap 5 | 5.x | 无需构建工具，响应式设计，上手快 |
| **模板引擎** | Jinja2 | Flask内置 | 语法简洁，与Flask完美集成 |

### 2.2 部署方案对比

| 方案 | 复杂度 | 适用性 | 推荐度 |
|-----|-------|-------|-------|
| **裸机部署** | 低 | 适合当前场景 | ⭐⭐⭐⭐⭐ |
| **Docker部署** | 中 | 便于迁移和版本管理 | ⭐⭐⭐⭐ |

**建议：裸机部署**  
理由：
- 服务器专用，无需隔离其他应用
- 裸机部署更简单，排查问题更直接
- 10人规模不需要容器编排的复杂度
- 备份只需复制SQLite文件和代码即可

### 2.3 服务器软件栈

| 组件 | 用途 |
|-----|------|
| Gunicorn | WSGI HTTP服务器，运行Flask应用 |
| Nginx（可选） | 反向代理、静态文件服务、HTTPS支持（如需） |
| systemd | 进程守护，确保服务开机自启 |

---

## 3. 系统架构设计

### 3.1 架构图

```
┌─────────────────────────────────────────────────────────┐
│                      用户浏览器                          │
│                   (IP:5000 访问)                         │
└───────────────────────┬─────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────┐
│                   VPS (Ubuntu)                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  Nginx (可选) :80/:443 → :5000                   │   │
│  └─────────────────────────────────────────────────┘   │
│                        │                                │
│  ┌─────────────────────▼───────────────────────────┐   │
│  │  Gunicorn (WSGI)                                │   │
│  │  ┌─────────────────────────────────────────┐   │   │
│  │  │  Flask Application                      │   │   │
│  │  │  ├── 订单录入模块                        │   │   │
│  │  │  ├── 查询检索模块                        │   │   │
│  │  │  ├── Excel导出模块                       │   │   │
│  │  │  └── 数据管理模块                        │   │   │
│  │  └─────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                        │                                │
│  ┌─────────────────────▼───────────────────────────┐   │
│  │  SQLite Database (单文件)                        │   │
│  │  └── /opt/erp/data/erp.db                       │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │  静态资源 & Excel模板                             │   │
│  │  └── /opt/erp/static/templates/order_template.xlsx│  │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 3.2 数据模型设计（初版）

```
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│   Company       │       │    Product      │       │    Order        │
│   (公司信息)     │       │   (产品信息)     │       │   (订单信息)     │
├─────────────────┤       ├─────────────────┤       ├─────────────────┤
│ id (PK)         │◄──────┤ company_id (FK) │       │ id (PK)         │
│ name            │       │ id (PK)         │◄──────┤ product_id (FK) │
│ address         │       │ name            │       │ company_id (FK) │
│ contact_person  │       │ product_no      │       │ quantity        │
│ phone           │       │ description     │       │ order_date      │
│ created_at      │       │ unit_price      │       │ delivery_date   │
└─────────────────┘       │ created_at      │       │ status          │
                          └─────────────────┘       │ remark          │
                                                    │ created_by      │
                                                    │ created_at      │
                                                    └─────────────────┘
```

---

## 4. 功能模块规划

### 4.1 模块划分

| 模块 | 功能点 | 优先级 |
|-----|-------|-------|
| **基础数据** | 公司信息增删改查 | P0 |
| | 产品信息增删改查 | P0 |
| **订单管理** | 订单录入 | P0 |
| | 订单列表/详情 | P0 |
| | 订单编辑/删除 | P1 |
| **查询检索** | 按产品查询 | P0 |
| | 按公司查询 | P0 |
| | 按日期范围查询 | P0 |
| | 历史订单追溯 | P0 |
| **报表导出** | 单订单导出Excel | P0 |
| | 批量订单导出 | P1 |
| | 预设格式打印模板 | P0 |
| **系统管理** | 简单登录（可选） | P2 |
| | 数据备份 | P1 |

### 4.2 页面规划

| 页面 | 路由 | 说明 |
|-----|------|------|
| 首页/仪表盘 | `/` | 快捷入口、最近订单 |
| 订单录入 | `/order/new` | 表单录入新订单 |
| 订单列表 | `/orders` | 所有订单展示 |
| 订单详情 | `/order/<id>` | 单订单详情 |
| 综合查询 | `/search` | 多条件检索页面 |
| 公司管理 | `/companies` | 公司信息维护 |
| 产品管理 | `/products` | 产品信息维护 |

---

## 5. 项目结构

```
/opt/erp/
├── app/                        # 应用主目录
│   ├── __init__.py            # Flask应用初始化
│   ├── models.py              # 数据模型定义
│   ├── routes/                # 路由/视图模块
│   │   ├── __init__.py
│   │   ├── main.py            # 主页面路由
│   │   ├── order.py           # 订单相关路由
│   │   ├── search.py          # 查询路由
│   │   └── export.py          # 导出路由
│   ├── services/              # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── order_service.py   # 订单业务逻辑
│   │   └── export_service.py  # 导出逻辑
│   └── utils/                 # 工具函数
│       ├── __init__.py
│       └── helpers.py
├── static/                    # 静态资源
│   ├── css/
│   ├── js/
│   └── templates/             # Excel模板文件
│       └── order_template.xlsx
├── templates/                 # HTML模板
│   ├── base.html             # 基础模板
│   ├── index.html
│   ├── order/
│   │   ├── list.html
│   │   ├── new.html
│   │   └── detail.html
│   ├── search/
│   │   └── index.html
│   └── components/
│       ├── navbar.html
│       └── pagination.html
├── data/                      # 数据目录
│   └── .gitkeep
├── logs/                      # 日志目录
│   └── .gitkeep
├── config.py                  # 配置文件
├── requirements.txt           # Python依赖
├── wsgi.py                    # WSGI入口
└── run.py                     # 开发运行入口
```

---

## 6. 部署方案

### 6.1 部署流程

```bash
# 1. 服务器准备
sudo apt update && sudo apt install -y python3-pip python3-venv nginx

# 2. 创建应用目录
sudo mkdir -p /opt/erp
sudo chown $USER:$USER /opt/erp
cd /opt/erp

# 3. 部署代码（Git克隆或上传）
git clone <repo> .

# 4. 创建虚拟环境
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# 5. 初始化数据库
flask db init
flask db migrate
flask db upgrade

# 6. 配置systemd服务
sudo vim /etc/systemd/system/erp.service
sudo systemctl enable erp
sudo systemctl start erp

# 7. 配置Nginx（可选，推荐）
sudo vim /etc/nginx/sites-available/erp
sudo ln -s /etc/nginx/sites-available/erp /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

### 6.2 备份策略

| 备份内容 | 方式 | 频率 |
|---------|------|------|
| SQLite数据库 | 定时复制+压缩 | 每日 |
| 代码文件 | Git版本控制 | 每次更新 |
| Excel模板 | 文件备份 | 变更时 |

**备份脚本示例**：
```bash
#!/bin/bash
# /opt/erp/backup.sh
cp /opt/erp/data/erp.db /backup/erp_$(date +%Y%m%d).db
tar czf /backup/erp_backup_$(date +%Y%m%d).tar.gz /opt/erp/
```

---

## 7. 技术风险与应对

| 风险点 | 影响 | 应对措施 |
|-------|------|---------|
| 数据量增长 | 单SQLite文件过大 | 设定归档策略，历史数据定期导出 |
| 并发写入冲突 | 数据丢失 | SQLite支持WAL模式，10人并发无压力 |
| 服务器宕机 | 服务中断 | systemd守护自动重启 |
| 数据丢失 | 业务中断 | 每日自动备份+异地备份 |

---

## 8. 开发计划（建议）

| 阶段 | 周期 | 交付物 |
|-----|------|-------|
| **Phase 1** | 3-5天 | 基础框架+订单录入+数据存储 |
| **Phase 2** | 2-3天 | 查询功能+列表展示 |
| **Phase 3** | 2-3天 | Excel导出（模板预设） |
| **Phase 4** | 1-2天 | 部署上线+测试优化 |

**预计总工期**：2周左右

---

## 9. 待确认事项

1. **Excel订单模板格式**：表格列、公司Logo、印章位置、字体样式等
2. **订单字段完整性**：除产品名、公司名、产品号、发货量外，还需要哪些字段？
3. **是否需要用户登录**：还是直接开放使用？
4. **VPS配置**：CPU/内存/带宽情况，用于优化Gunicorn worker数
5. **是否需要域名**：还是纯IP+端口访问？

---

## 10. 方案总结

本方案采用 **Python + Flask + SQLite** 技术栈，裸机部署于Ubuntu VPS，具有以下优势：

- ✅ **开发快速**：Flask轻量，无需复杂配置
- ✅ **部署简单**：单服务器即可运行，维护成本低
- ✅ **数据安全**：SQLite单文件，备份迁移方便
- ✅ **导出强大**：Python生态支持复杂Excel格式
- ✅ **扩展灵活**：如需迁移到MySQL或Docker，改动量小

---

*文档版本：v1.0*  
*创建日期：2026-03-04*  
*状态：待评审*
